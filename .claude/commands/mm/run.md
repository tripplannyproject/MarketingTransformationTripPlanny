---
description: Run a catalog route through the seven-phase MAMW marketing cycle with checkpointing.
---

# /mm:run — MAMW route runner

Read and follow `.claude/skills/mm-run/SKILL.md` exactly. This command is the canonical Claude
entry point; the `mm-run` skill (invocable as `/mm-run` once it loads) is the shared
implementation. Pass the route argument and context to that workflow. Do not define a second
workflow or weaken any gate.
