---
description: Resume an interrupted MAMW route run from the .mm-run.json checkpoint.
---

# /mm:resume — Resume interrupted MAMW run

Read and follow `.claude/skills/mm-resume/SKILL.md` exactly. This command is the canonical Claude
entry point; the `mm-resume` skill (invocable as `/mm-resume` once it loads) is the shared
implementation. Do not define a second workflow or weaken any gate.
