---
description: Onboard or reconfigure MAMW for this project (brand, markets, locales, channels, governance, packs).
---

# /mm:start — MAMW onboarding

Read and follow `.claude/skills/mm-start/SKILL.md` exactly. This command is the canonical Claude
entry point; the `mm-start` skill (invocable as `/mm-start` once it loads) is the shared
implementation. Do not define a second workflow or weaken any gate — pass the user's arguments and
context to that workflow.
